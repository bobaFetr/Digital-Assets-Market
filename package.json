{
  "name": "dam-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:web": "cd FrontEnd && npm run dev",
    "dev:api": "dotnet run --project BackEnd/MyWebApi",
    "dev:full": "npx concurrently -k -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "build:web": "cd FrontEnd && npm run build",
    "lint:web": "cd FrontEnd && npm run lint",
    "preview:web": "cd FrontEnd && npm run preview"
  }
}
